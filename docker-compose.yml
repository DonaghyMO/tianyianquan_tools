version: '3.8'
services:
  mysqldb:
    container_name: mysqldb
    image: mysql:8.1
    command: --default-authentication-plugin=mysql_native_password
    expose:
      - 3306
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: "1998"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always
    networks:
      - test_network
  #SpringBoot项目服务
  tianyianquan:
    container_name: tianyianquan
    #构建指定目录下的Dockerfile
    image: d01a371942a39f00f446be8eeb25e19ac3256c695c71132a74f52620ce39dd20
    depends_on:
      - mysqldb
    expose:
      - 8080
    ports:
      - "8080:8080"
    restart: always
    networks:
      - test_network
networks:
  test_network:
    driver: bridge